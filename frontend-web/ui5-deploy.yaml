# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: zpeweb
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://soalv3hsap01.stefanini.dom:8000
          client: '800'
        app:
          name: ZPE_ADV
          description: Projeto estagiarios
          package: ZPE_ADV_ODATA
          transport: SFDK901320
        exclude:
          - /test/
          - /localService/
