<mvc:View controllerName="zpeweb.controller.UnidadeMedida"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:f="sap.f"
    displayBlock="true">
    <Page 
        id="unidadePage" 
        title="Manutenção de Unidades de Medida"
        showNavButton="true"
        navButtonPress=".onNavBack"
        enableScrolling="true">

        <content>
            <VBox width="50%" class="sapUiSmallMargin">
                <OverflowToolbar height="5rem" class="sapUiMediumMarginBottom">
                    <Title text="Cadastro de Unidade de Medida"/>
                    <ToolbarSpacer/>
                    <Button 
                        icon="sap-icon://add" 
                        text="Nova Unidade" 
                        type="Emphasized" 
                        press=".onCriarUnidade" 
                        class="sapUiTinyMarginEnd"/>
                    <Button 
                        text="Excluir" 
                        icon="sap-icon://delete" 
                        type="Reject" 
                        press="onDeletarUnidade" />
                </OverflowToolbar>

                <Table 
                id="tableUnidades" 
                items="{/ZTPE_UNMEDIDASet}" 
                mode="MultiSelect" 
                width="100%"
                alternateRowColors="true">
                    <headerToolbar>
                        <OverflowToolbar>
                            <Title text="Unidades Cadastradas"/>
                            <ToolbarSpacer />
                            <Button 
                                icon="sap-icon://edit" 
                                text="Editar" 
                                visible="{= !${viewModel>/modoEdicao}}"
                                press=".onAlternarEdicao"/>

                            <Button 
                                icon="sap-icon://edit"
                                text="Salvar"
                                type="Accept"
                                visible="{viewModel>/modoEdicao}"
                                press=".onSalvarAlteracoes"/>
                            <Button
                                icon="sap-icon://decline"
                                text="Cancelar"
                                type="Reject"
                                visible="{viewModel>/modoEdicao}"
                                press=".onCancelarAlteracoes"/>
                        </OverflowToolbar>
                    </headerToolbar>
                    <columns>
                        <Column id="colSigla" width="100px" ><Text id="txtSigla" text="Sigla"/></Column>
                        <Column id="colNomeTecnico"><Text id="txtNomeTecnico" text="Descrição" /></Column>
                    </columns>
                    <items>
                        <ColumnListItem id="itemUnidade">
                            <cells>
                                <Text text="{Codigounm}" />
                                <Input value="{Descricaounm}" width="auto" maxLength="30" editable="{viewModel>/modoEdicao}" valueLiveUpdate="true"/>
                            </cells>
                        </ColumnListItem>
                    </items>            
                </Table>
            </VBox>
        </content>
    </Page>
</mvc:View>